# B2 — Shop layout and capacity delivery

September 22, 2026. **B2 technically complete. Stop before B3. Owner acceptance pending; not beta ready.**

B2 implements preparation-only rack purchase and movement in the ordinary illustrated encounter. Pointer placement, 10px arrow adjustment, preview, Enter/Confirm and Escape/Cancel are available beside the rack selector. Invalid placement explains bounds, overlap, protected aisle, blocked port/route, cash, rack limit, fixed station, phase or stale request. Pending work must finish/cancel first; previews disable unrelated business inputs. Placement covering Rowan is rejected.

`layout.gd` is the shared source for fixture origins, floor footprints, artwork offsets, four local case anchors, ports, slots and four-neighbor navigation. `state.gd` owns stable fixture/copy/slot identities, acquisition provenance, layout revisions and unique paid-event identities. Buy assigns rack-0002, debits exactly $60 and expands capacity from four to eight. Moving is free. Reservations retain their slot until sale or return; stocked movement and reload preserve identities, labels and historical cost. Scene work captures copy, slot, rack, revision, product and price before walking; stale/unreachable work cannot complete remotely.

Schema **6**, ruleset **b2-layout-1**, uses **game-sim-first-week-dev-v6**. Purchase confirmations remain idempotent after reload. Cash reconstructs from opening cash plus paid sales minus supplier and fixture events; orders/sales reference the same authoritative events rather than being charged again. Rack investment is displayed separately from merchandise margin. JSON parsing and candidate validation reject malformed/old saves without changing live state or the file. No migration is provided.

Opening cash stays **$550**, with **three prepaid starter copies**, **three original titles and reference prices**, three staggered visitors, and the existing report-order/next-day-receiving timing. Six starter copies, changed reference prices, seven-day economy and new release ordering are not part of B2.

## Source and preservation

Branch `main`, HEAD `22ac1bcbf6f4cc65fd7425508a1f471e2306dff9`, **plus uncommitted changes**. HEAD alone is not the delivered candidate. The [source manifest](../../encounter/evidence/b2-20260922/source-manifest.json) identifies the active encounter, relevant documentation and external root tracker; the [final validation context](../../encounter/evidence/validation-20260922T194446951813Z/context.json) identifies every copied encounter file and Godot 4.6.2 engine. Executable/runtime/assets match that tested context with zero mismatches. Later documentation reconciliation is included in the delivery manifest.

The incoming uncommitted B1 packet, master plan and milestone documents, plus root tracker, are preserved in [incoming-b1](../../encounter/evidence/b2-20260922/incoming-b1/manifest.json) before extension. [Preservation check](../../encounter/evidence/b2-20260922/preservation-check.json) confirms **81 artwork/master/import/rig files** still match the September 21 source context. No artwork was regenerated or overwritten: retained quarter-scale exports were aligned through shared offsets. Owner save directories and historical evidence were not accessed or modified. No commit, push or publication occurred.

## Geometry correction

The proposed y=580 queue left no usable passing route for a sold buyer behind waiting bodies. The retained [failed drain](../../encounter/evidence/validation-20260922T192957283672Z/assortment-scene.log) shows the first buyer stuck after payment. The shared specification, counter buyer port and queue now use **(800,570), (720,570), (640,570)**. Static clearance remains a **12px actor-foot radius**. Runtime buyer-center separation is **28px**, leaving a passing lane at y=600 inside the original floor. Exits are processed before queues and incoming browsers; admission waits for leaving/unsettled queued actors. Temporary occupied routes wait without transactions.

All other B1 fixture origins, sizes, floor bounds and slot anchors remain as proposed. The required stocked move **(440,330) → (430,330)** works with rack-0002 at **(400,430)**. A nonoverlapping candidate with rack-0001 at **(620,360)** and rack-0002 at **(400,430)** disconnects required ports and is rejected. Connectivity uses the same navigation grid as the scene. Single connected-component checking proves the required bidirectional paths on this undirected grid; dynamic movement is verified separately.

## Acceptance evidence

Final isolated run: [validation-20260922T194446951813Z](../../encounter/evidence/validation-20260922T194446951813Z/). All **10 engine processes exited 0**, with no script/engine errors or warnings in their logs. Passed suites: **110 assortment state**, **52 layout state**, **3 pending-price**, **54 assortment scene** and **48 layout scene** checks. Both scene suites passed headless, normal 1280×720 and Retina 2560×1440. [Four wrapper tests](../../encounter/evidence/b2-20260922/wrapper-tests.log) pass, including rejection of the old R5 namespace, duplicate save-directory assignments and disabled isolation.

| B2 acceptance case | Result and retained evidence |
| --- | --- |
| Fresh schema 6 | Four derived slots, fixed receiving/checkout, $550 and zero fixture expense; receiving creates the original three copies. [State results](../../encounter/evidence/validation-20260922T194446951813Z/results/layout-state.json). |
| Buy confirmation | $550 → $490, rack-0002, eight slots, one $60 paid event; repeated confirmation and repeated request after reload are inert. Also buys correctly after prior supplier/sale events. State and native results. |
| Cancel / reject | Preview/cancel is pure; insufficient cash reached through real supplier spending, overlap, bounds, aisle, port, disconnected route, fixed/unknown fixture, fractional/unsnapped origin, stale request and third rack reject without changing state. Native pointer/keyboard cancel and red preview verified. |
| Stocked move | Required 10px move retains every copy field and reloads exactly; sprite origin and work port shift together. [Normal moved rack](../../encounter/evidence/validation-20260922T194446951813Z/results/b2-stocked-moved-normal.png), state/scene results and native checkpoint. |
| Capacity | Four/eight boundaries; explicit slot assignment and double-use rejection; held copies occupy slots; sale and return release them; refusal restores the original slot. State tests plus ordinary four-slot regression day. |
| Ordinary altered-layout day | Receive, label, select/stock both racks, open, browse, reserve, queue, serve, close/drain, report, order six copies, advance and receive again. $490 + $56.97 sales − $50 order = **$496.97**, six day-2 backroom copies and eight slots. [Scene results](../../encounter/evidence/validation-20260922T194446951813Z/results/layout-scene-normal.json). |
| Routing | Shared port/connectivity tests and a nonoverlapping disconnected rejection. Every-frame fixture/separation observation and movement-distance checks during rendered flow; occupied-aisle probe waits without remote completion. [Motion trace](../../encounter/evidence/validation-20260922T194446951813Z/results/layout-motion-normal.json). |
| Pending action | Captured product/price/rack/copy/slot remain fixed despite later UI selection changes; changed revision rejects at arrival; an explicitly empty unreachable path cannot perform; editor refuses active work. Price and layout scene suites. |
| Phase / save | Prep-only edits; open/closing/report rejected; player-foot covering rejected; draft excluded from save; bought/stocked/moved/reserved/report saves reload; malformed shapes, JSON, schema 5, slots, provenance, event/cash/revision corruption rejected atomically. |
| Visual interaction | Green/red ghost, selected slot overlays, bought rack and stocked movement inspected at both sizes. Native macOS pointer placement, arrow nudge, Escape cancel, Enter confirm, physical stocking/move and K/L save/reload additionally exercised. Customer motion inspected through the recorded sequence, native live session and traces, not screenshots alone. |

[41-second rendered interaction video](../../encounter/evidence/validation-20260922T194446951813Z/b2-interaction.mp4) contains 246 frames at 1280×720. The scripted session uses actual viewport pointer/key events and runs at 3× simulation time; it also deliberately injects clearly scoped stale-revision/unreachable/temporary-obstruction probes. Those probes are engineering checks, not ordinary owner actions. Inspected normal frames include moving customers browsing, traveling to the queue, passing exiting buyers and draining checkout; normal and Retina images are retained in `results/`.

The separate [native input result](../../encounter/evidence/b2-20260922/native-result.json) uses macOS pointer/keyboard through the app, with an [observer](../../encounter/evidence/b2-20260922/native-observer.gd) that records but supplies **no gameplay inputs**. It canceled and bought a rack, stocked and moved rack-0001, saved/reloaded, opened, observed all three moving visitors, served Alex, closed/drained, finalized and saved. The deliberately sparse one-copy display produced **one $21.99 sale, two stock misses and $511.99 cash**. All three visitors traveled more than 776 sampled logical pixels; sampled fixture intersections were zero. The final [synthetic checkpoint](../../encounter/evidence/b2-20260922/native/checkpoint.json), motion trace and state-change screenshots are retained. Native process exit was 0 with no warnings/errors. This is agent verification, not owner acceptance.

## Failures, limits and B3 handoff

Earlier attempts remain retained rather than relabeled as passes: parser/JSON normalization failures, the blocked checkout drain, host CoreAudio startup failure, and a rendered input miss before test helpers began moving the pointer before pressing. The final runner uses **Dummy audio** for isolated checks because this host rejected CoreAudio startup; graphical/input/routing evidence is muted and does not qualify audio. Malformed JSON now uses the error-returning parser instead of emitting an engine error for a handled load failure. The validator still rejects engine/script errors and ambiguous isolation settings.

B2 does not accept the inherited rigid/deforming legs, turns/stops/reach, foot sliding, mirrored lighting or reused rigs. There is no expanded room, worker simulation, used trading, new release content, bills, bankruptcy, seven-day endpoint or packaged Mac beta. No generic save migration/autosave/backup system was added. The historical capture-exit ObjectDB warning remains a B9 investigation; clean logs here do not resolve its ordinary-play relevance. R1–R4 acceptance stays bounded; R5 and B2 owner verdicts remain pending. Beta acceptance remains B10.

**Next actor: the owner may authorize B3 engineering. No further B2 work or B3 implementation is running.** B3 starts from this exact source and the updated B1 geometry. It owns schema 7 in its separate development namespace, north-bay expansion and rack-0003, $100 exactly-once expansion, visible due bills, settlement/insolvency and the seven-day terminal result. Extend the existing layout/event/slot paths, preserving B2 and older namespaces. Revalidate included-rack placement before charging; actual employment/wages integrate in B4 and content/order-timing changes stay B6. Keep technical results and owner judgments separate. **Stop before B3.**
