# Native R5 review

Computer Use selected the separate `local.game-sim.r5-review` app identity. Prior R3/R4 windows were left untouched. The bundle is a copied official Godot 4.6.2 app with a separate identifier and ad-hoc development signature; original/signed executable hashes and PIDs are recorded in native-launch.json and owner-launch.json. Ordinary Launch Encounter.command continues to use the installed official Godot app.

Native actions: received the prepaid three-product shipment, applied Curb $21.99, selected Tide from the actual dropdown, typed $19.99, applied it, selected Orbit and applied $14.99. Opened Assortment, stocked one of each, returned Tide to the backroom, observed its retained identity and stock counts, restocked it, closed the dialog, saved and reloaded. The Orbit price field restored as $14.99 while Orbit remained selected. Opened admission and observed three separate visitors queue with their correct products/offers.

After the comparison-only setup was finalized, saved this newly created R5 checkpoint and gracefully closed only this session's R5 app. Retained the checkpoint and log, launched final source in the same separate bundle, and used native L to restore. Final visible state: OPEN, $550 cash, Alex / Curb Circuit 02 / queue 1 / $21.99; Blair / Tidebound Atlas / queue 2 / $19.99; Casey / Orbit Orchard / queue 3 / $14.99. The primary control reads Serve Alex · $21.99. No native checkout was performed; owner can operate the queue. The ordinary saved checkpoint is this newly created R5 review state, not any prior namespace.

Native display evidence: window 2560×1440, logical viewport 1280×720, screen and canvas scale 2.0, Apple M3 Pro. Native screenshots in the Computer Use transcript were inspected; retained scene captures independently cover normal and Retina UI. Rendered scene tests use actual button/keyboard input, with programmatic dropdown selection and numeric field setup; native actions specifically verify dropdown and typed pricing. Capture uses a documented guided adapter, not owner input.

Animation limitations remain: rigid/deforming legs, abrupt turns/stops, straight reach, possible sliding, mirrored lighting, enlarged-outline artifacts, reused/tinted rigs and no detailed cash handoff. The capture-exit ObjectDB leak warning remains unresolved. Native launch does not establish warning-free shutdown. R5 remains subject to owner review.

Final handoff update: after the pending-label and recording fixes, a fresh `local.game-sim.r5-owner-final` development identity was launched from the exact final runtime. Native L restored the retained open checkpoint successfully (`owner-handoff.log`), and `owner-launch.json` binds PID/source/engine identity. Computer Use returned stale/tiny images after this final relaunch, so no new final-native pixel claim is made from those images. The earlier native dropdown/typing/return/reload operation and final normal/Retina scene captures provide visual/control evidence. The final process remains running; the redundant earlier session-created R5 review process was stopped only after its saved checkpoint was restored. Earlier R3/R4 processes and namespaces were untouched.
