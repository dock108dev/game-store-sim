# Native R4 owner handoff

2026-09-08 local / 2026-09-09 UTC. Native Retina operation verified in `Replay Junction • R4 customer wave (DEBUG)` using Computer Use. The tool selected a separate development copy of the installed Godot app so the existing R3 window and its state stayed untouched. Bundle identity, engine signing details and PID are in `native-launch.json`; the official ordinary launcher remains unchanged.

Native clicks received three copies; keyboard input entered $21.99; native controls printed labels, stocked and opened. The final observed window shows OPEN, cash $550.00, shelf 1, sold 0, Alex queue #1, Blair price declined, Casey queue #2, and `Serve Alex · $21.99`. There are two separate visible waiting customers. No checkout, close or report action was taken in this owner review shift. The owner can now operate it. The native log records actual window 2560×1440, logical viewport 1280×720 and canvas/screen scale 2.0.

This is readiness and agent operation only. R4 owner acceptance remains pending. The R3 “build on” feedback is bounded R3 acceptance. Earlier R3 state and windows were preserved. The initial new R4 prep process created by this session was replaced only to establish the separate native review app identity.

Animation remains the inherited directional reused rig: rigid/deforming legs, abrupt turns/stops, straight reach, possible sliding, mirrored lighting, enlarged-outline artifacts and no detailed payment handoff. Ordinary capture retains an ObjectDB leak warning at exit; this is documented, not suppressed. Normal and Retina gameplay suites pass, with no script errors.
