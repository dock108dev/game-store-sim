# Visual Tests

Owns UI, layout, readability, scene-composition, screenshot, and visual-state
tests. Store reusable expected output under `tests/baselines` instead of
embedding large golden data in test scripts.
