# Visual Tests

Owns UI, layout, readability, scene-composition, screenshot, and visual-state
tests. Shared expected-output fixtures live under `tests/baselines`; reviewed
store-sweep PNG baselines live under `tests/visual/baselines` because
`scripts/run_store_visual_sweep.sh` reads that path by default.
