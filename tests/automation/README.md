# Test Automation

Owns test runners, GUT hooks, scenario helpers, fixtures for automation, and
CI validation helpers. Executable regression tests should live in `tests/unit`,
`tests/integration`, `tests/flows`, or `tests/visual` unless they are
automation self-tests.
