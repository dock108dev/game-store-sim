# Flow Tests

Owns end-to-end player, store-session, and scenario tests that read like
playable routes. Keep narrow unit checks in `tests/unit` and multi-system
fixture checks in `tests/integration`.
