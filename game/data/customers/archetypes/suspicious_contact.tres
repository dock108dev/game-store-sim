[gd_resource type="Resource" script_class="CustomerArchetype" load_steps=2 format=3]

[ext_resource type="Script" path="res://scripts/customers/customer_archetype.gd" id="1_customer_archetype"]

[resource]
script = ExtResource("1_customer_archetype")
archetype_id = "suspicious_contact"
display_name = "Suspicious Contact"
role = "suspicious"
preferred_categories = Array[String](["used_game", "bulk_lot"])
price_sensitivity = 1.35
patience_seconds = 5.0
visual_cue = "cash stack, note, hood"
dialogue_style = "quiet hidden-thread probe"
dialogue_flow_ids = Array[String](["hidden_thread_probe"])
default_feedback = "Cash is easier if we skip the receipt."
arrival_line = "They stay off the main queue, watching the backroom door and register traffic."
satisfaction_line = "Any hidden-thread choice should feel optional and separate from normal retail work."
friction_line = "Refusing the deal keeps the store clean but may close off a supplier rumor."
hidden_thread_contact = true
